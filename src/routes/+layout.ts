// paper prototype mode: disable server-side rendering
// everything runs client-side with localStorage instead of a real database
// remove these two lines when you switch to a real backend
export const ssr = false;
export const prerender = false;
